import { HeaderNext, HeaderTitle } from '@ogcio/design-system-react';

export function Header() {
  return (
    <HeaderNext variant="default" aria-label="ListLens site header">
      <HeaderTitle>ListLens</HeaderTitle>
    </HeaderNext>
  );
}
